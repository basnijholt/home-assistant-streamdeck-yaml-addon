---
name: CI

# yamllint disable-line rule:truthy
on:
  push:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  workflows:
    uses: basnijholt/workflows/.github/workflows/addon-ci.yaml@main
    with:
      lint_yamllint: false
